---
title: Blog
view: article-grid
# Hidden for now — no posts ready to publish. To unhide:
#   1. remove this `build` and `cascade` block, and
#   2. uncomment the Blog entry in config/_default/menus.yaml.
build:
  render: never
  list: never
cascade:
  build:
    render: never
    list: never
---

Notes, tutorials and notebook-style write-ups.
